#!/bin/bash

# Script to checkout all the branches of the team
git checkout -b jobo origin/cine
git checkout -b jobo origin/jaan
git checkout -b jobo origin/jobo
git checkout -b kewe origin/kewe
git checkout -b lyke origin/lyke
git checkout -b ribi origin/ribi